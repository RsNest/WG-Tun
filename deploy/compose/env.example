# Copy to .env next to the compose file, or export in the shell.
# Use an immutable tag. Do not deploy from the mutable "main" tag.
# PROXYCTL_VERSION=v0.1.0
PROXYCTL_VERSION=sha-replace-with-short-commit
